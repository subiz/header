syntax = "proto3";

package email;

message Email {
	string From = 1;
	string To = 2;
	string Subject = 3;
	string Body = 4;
	string JSONData = 5;
	repeated string AttachLinks = 6;
}

message Empty {}

service EmailSender {
	rpc Send(Email) returns (Empty) {}
}

