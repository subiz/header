syntax = "proto3";

package partitioner;

message Configuration {
	string version = 1;
	int32 term = 2;
	// ID of the cluster
	string cluster = 3;

	//	repeated WorkerConfiguration workers = 5;
	int32 total_partitions = 6;
	int32 next_term = 7;
	map<string, WorkerPartitions> partitions = 9;
	map<string, string> hosts = 10;
}

message WorkerHost {
	string id = 4;
	string host = 5;
}

message WorkerPartitions {
	string id = 4;
	repeated int32 partitions = 7;
}

message Empty {}

service Coordinator {
	rpc GetConfig(Empty) returns (Configuration) {}
	rpc Join(WorkerHost) returns (Empty) {}
}

service Worker {
	rpc GetConfig(Empty) returns (Configuration) {}
	rpc GetHost(Empty) returns (WorkerHost) {}
	rpc Prepare(Configuration) returns (Empty) {}
}
