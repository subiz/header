syntax = "proto3";

package callback;

message Task {
	string id = 2;

	// callback_sec is second*, golang: time.Now().Unix(),
	// node: new Date() * 1000000
	int64 callback_sec = 3;

	// kafka topic
	string topic = 4;

	// Data is value of kafka event in bytes
	bytes data = 5;

	// kafka key
	string key = 6;

	// kafka partition
	int32 partition = 7; // -1, auto calculate using key

	int64 called = 8; // internal use only
}