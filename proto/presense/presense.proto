syntax = "proto3";

package presense;

message Id {
	string Id = 1;
}

message Empty {}

message LastFocusTime {
	string TimeSpan = 1;
}

service PresenseMgr {
	rpc Ping(Id) returns (Empty) {}
	rpc Bye(Id) returns (Empty) {}
	rpc LastFocusingTime(Id) returns (LastFocusTime) {}
}