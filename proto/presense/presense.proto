
syntax = "proto3";

package presense;

message UserId {
	string Id = 1;
}

message Empty {}

message LastFocusTime {
	string TimeSpan = 1;
}

service PresenseMgr {
	rpc Ping(UserId) returns (Empty) {}
	rpc Bye(UserId) returns (Empty) {}
	rpc LastFocusingTime(UserId) returns (LastFocusTime) {}
}