syntax = "proto3";

package auth;
import "bitbucket.org/subiz/servicespec/proto/common/auth/scope.proto";

message Credential {
	string account_id = 1;
	string user_id = 2;
	
	auth.Scope user_scope = 3;

	// scope that the user allow
	auth.Scope scope = 4;

	bool internal = 5;
}
