syntax = "proto3";

package cluster;

service ClusterChangeHandler {
	rpc UpdateCluster(Cluster) returns (Empty) {}
}

message Empty {}

message Cluster {
	string Service = 1;
	repeated string Hosts = 2;
}